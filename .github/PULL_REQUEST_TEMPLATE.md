## Summary

<!-- What does this PR do and why? -->

## Related

<!-- Phase / issue / spec section, e.g. BUILD-SPEC Phase 9 -->

## Checklist

- [ ] `pnpm typecheck && pnpm lint && pnpm test && pnpm build` passes locally
- [ ] Follows RULES R1–R10 (RSC default, Server Actions, zod, RLS, no `any`, no hardcoded strings)
- [ ] No locked structural files edited (or a gap is recorded in `docs/template-gaps.md`)
- [ ] `messages/th.json` and `messages/en.json` stay in key parity
- [ ] Schema changes are new files in `supabase/migrations/`
- [ ] Docs updated if behavior changed

## Screenshots / notes

<!-- Optional -->
